# 🔥 ЗАДАЧА: Полное программное тестирование Yalla Business Admin

---

## 🔐 ТЕСТОВЫЕ АККАУНТЫ (DEV)

### SUPER_ADMIN (полный доступ ко всем компаниям):
| ФИО | Телефон | Пароль | Email | Компания |
|-----|---------|--------|-------|----------|
| Азамат (супер-админ) | `+992876086666` | `azamat6666` | rasuliazamat@gmail.com | Yalla Headquarters |

### Админы компании "Ориёнбанк":
| ФИО | Телефон | Пароль | Проект |
|-----|---------|--------|--------|
| Фируз Каримов | `+992917000001` | `azamat6666` | Головной офис |
| Шохин Рахимов | `+992917000002` | `azamat6666` | Филиал Худжанд |
| Манижа Саидова | `+992917000003` | `azamat6666` | Филиал Куляб |
| Бахром Назаров | `+992917000004` | `azamat6666` | Филиал Бохтар |

### Админы компании "TALCO":
| ФИО | Телефон | Пароль | Проект |
|-----|---------|--------|--------|
| Сорбон Давлатов | `+992917000007` | `azamat6666` | Завод Турсунзаде |
| Фаррух Исмоилов | `+992917000008` | `azamat6666` | Офис Душанбе |

### Админы компании "TechnoStart":
| ФИО | Телефон | Пароль | Проект |
|-----|---------|--------|--------|
| Азиз Комилов | `+992917000010` | `azamat6666` | Технопарк |
| Нигина Шарипова | `+992917000011` | `azamat6666` | IT Park |

### Админы компании "МегаФон":
| ФИО | Телефон | Пароль | Проект |
|-----|---------|--------|--------|
| Джамшед Рахмонов | `+992917000013` | `azamat6666` | Центральный офис |
| Ситора Файзуллоева | `+992917000014` | `azamat6666` | Филиал Согд |
| Мухаммад Олимов | `+992917000015` | `azamat6666` | Филиал Хатлон |

---

## 🎯 ЦЕЛЬ

Ты — QA/Dev агент. Твоя задача — **полностью протестировать** проект Yalla Business Admin и подготовить его к production.

**Что нужно сделать:**
1. Протестировать ВСЕ 98 тест-кейсов из `bpv/FEATURES_DOCUMENTATION.md`
2. Найти баги и исправить их
3. Убедиться что всё работает
4. Написать финальный отчёт

**У тебя полный карт-бланш:**
- Можешь создавать любые файлы если это нужно
- Можешь менять код
- Можешь запускать любые команды
- Можешь деплоить

**Главное — результат: полностью протестированный и рабочий продукт.**

---

## 🔧 ДОСТУПНЫЕ ИНСТРУМЕНТЫ

### 1. База данных (Supabase DEV)
```
mcp_supabase_execute_sql   — выполнить SQL запрос
mcp_supabase_list_tables   — посмотреть таблицы
mcp_supabase_get_logs      — логи
```

### 2. Backend API
**URL:** `https://business-crm-iu04.onrender.com/api`

Можешь тестировать API через:
- Терминал (curl)
- Создать тестовый скрипт
- Напрямую через fetch в коде

### 3. Деплой
```
mcp_vercel_deploy_to_vercel  — деплой frontend
mcp_render_list_services     — сервисы Render
mcp_render_list_deploys      — деплои
mcp_render_get_logs          — логи backend
```

### 4. Код проекта
- Читай любые файлы
- Редактируй что нужно
- Создавай новые если надо

---

## 📚 ДОКУМЕНТАЦИЯ

**Обязательно прочитай:**
- `bpv/FEATURES_DOCUMENTATION.md` — 98 тест-кейсов которые нужно проверить

**Структура проекта:**
```
backend/
├── src/YallaBusinessAdmin.Api/Controllers/    — API endpoints
├── src/YallaBusinessAdmin.Infrastructure/Services/  — бизнес-логика
├── src/YallaBusinessAdmin.Domain/Entities/    — модели данных

frontend/
├── app/(dashboard)/     — страницы
├── components/          — компоненты
├── lib/api/            — API клиенты
├── stores/             — Zustand stores
├── lib/features.config.ts  — feature flags
```

---

## 📋 ЧТО ТЕСТИРОВАТЬ

### Фича 1: Аутентификация (12 тестов)
**API endpoints:**
```
POST /api/auth/login           — вход
POST /api/auth/refresh         — обновление токена
POST /api/auth/logout          — выход
GET  /api/auth/me              — текущий пользователь
PUT  /api/auth/profile         — обновить профиль
POST /api/auth/change-password — сменить пароль
POST /api/auth/forgot-password — забыл пароль
POST /api/auth/reset-password  — сброс пароля
POST /api/auth/impersonate/{id} — импершонация (SUPER_ADMIN)
POST /api/auth/stop-impersonation — выйти из импершонации
```

**Что проверить:**
- Вход с правильным паролем → 200 + токены
- Вход с неправильным паролем → 401
- Вход заблокированного пользователя → 401
- Refresh token работает
- Импершонация только для SUPER_ADMIN
- Изоляция: админ одной компании не видит данные другой

---

### Фича 2: Пользователи (11 тестов)
**API endpoints:**
```
GET    /api/users              — список с пагинацией и фильтрами
GET    /api/users/{id}         — пользователь по ID
POST   /api/users              — создать
PUT    /api/users/{id}         — обновить
DELETE /api/users/{id}         — удалить (soft delete)
GET    /api/users/all-admins   — все админы (SUPER_ADMIN only)
GET    /api/users/permissions/routes — доступные роуты
GET    /api/users/statuses     — статусы
GET    /api/users/roles        — роли
```

**Что проверить:**
- CRUD операции работают
- Фильтры (status, role, search) работают
- Уникальность phone и email
- Soft delete работает
- Изоляция по companyId
- Нельзя удалить себя
- Нельзя удалить последнего admin

---

### Фича 3: Сотрудники (16 тестов)
**API endpoints:**
```
GET    /api/employees              — список
GET    /api/employees/{id}         — детали
POST   /api/employees              — создать
PUT    /api/employees/{id}         — обновить
DELETE /api/employees/{id}         — удалить
PATCH  /api/employees/{id}/activate — активировать/деактивировать
PUT    /api/employees/{id}/budget  — обновить бюджет
PUT    /api/employees/budget/batch — массовое обновление бюджета
GET    /api/employees/{id}/orders  — история заказов
GET    /api/employees/export       — экспорт CSV
GET    /api/employees/invite-statuses — статусы приглашений
```

**Что проверить:**
- CRUD работает
- Привязка к проекту обязательна
- Уникальность phone
- Toggle activation работает
- Бюджеты (single и batch)
- Фильтры (status, invite, budget, subscription)
- Экспорт CSV (UTF-8, кириллица)
- Soft delete

---

### Фича 4: Проекты (8 тестов)
**API endpoints:**
```
GET    /api/projects           — список
GET    /api/projects/{id}      — детали
POST   /api/projects           — создать
PUT    /api/projects/{id}      — обновить
DELETE /api/projects/{id}      — удалить
GET    /api/projects/{id}/stats — статистика
GET    /api/projects/service-types — типы услуг
```

**Что проверить:**
- CRUD работает
- **АДРЕС НЕИЗМЕНЯЕМ после создания!**
- HQ флаг работает
- Изоляция по companyId
- Cutoff time на уровне проекта

---

### Фича 5: Dashboard (13 тестов)
**API endpoints:**
```
GET  /api/home/dashboard           — статистика
GET  /api/home/orders              — заказы с фильтрами
POST /api/home/guest-orders        — гостевой заказ
POST /api/home/assign-meals        — назначить обеды
POST /api/home/bulk-action         — массовые действия (pause/resume/cancel)
PUT  /api/home/subscriptions/{id}  — обновить подписку
POST /api/home/subscriptions/bulk  — массовое обновление
GET  /api/home/cutoff-time         — время отсечки
PUT  /api/home/cutoff-time         — изменить cutoff
GET  /api/home/combos              — типы комбо (25/35)
GET  /api/home/orders/export       — экспорт заказов CSV
```

**Что проверить:**
- Dashboard статистика корректна
- Заказы фильтруются по дате/статусу/типу
- Гостевые заказы создаются
- Bulk actions работают
- **CUTOFF блокирует изменения на "сегодня" после времени отсечки!**
- Экспорт CSV

---

### Фича 6: Подписки Lunch (15 тестов)
**API endpoints:**
```
GET  /api/meal-subscriptions               — список
GET  /api/meal-subscriptions/{id}          — детали
POST /api/meal-subscriptions               — создать
POST /api/meal-subscriptions/{id}/cancel   — отменить
POST /api/meal-subscriptions/{id}/pause    — приостановить
POST /api/meal-subscriptions/{id}/resume   — возобновить
POST /api/meal-subscriptions/price-preview — предпросмотр цены
GET  /api/meal-subscriptions/{id}/assignments      — назначения
GET  /api/meal-subscriptions/employees/{id}/assignments — назначения сотрудника
PUT  /api/meal-subscriptions/assignments/{id}      — обновить
POST /api/meal-subscriptions/assignments/{id}/cancel — отменить назначение
POST /api/meal-subscriptions/assignments/{id}/freeze — заморозить
POST /api/meal-subscriptions/assignments/{id}/unfreeze — разморозить
GET  /api/meal-subscriptions/employees/{id}/freeze-info — инфо о заморозках
GET  /api/meal-subscriptions/calendar      — календарь
```

**Что проверить:**
- Подписки создаются/отменяются
- Pause/Resume работает
- **ЛИМИТ ЗАМОРОЗОК: 2 в неделю!**
- Freeze/Unfreeze работает
- Уникальность (employee_id + date)
- Нельзя менять прошлые даты

---

### Фича 7: Гостевые заказы (6 тестов)
**Что проверить:**
- Создание гостевого заказа
- Блокировка при бюджете = 0
- Блокировка после cutoff
- Заказ на будущую дату
- Деньги списываются сразу (невозвратно)
- Гостевые нельзя замораживать

---

### Фича 8: Транзакции (4 теста)
**API endpoints:**
```
GET /api/transactions          — список
GET /api/transactions/{id}     — детали
GET /api/transactions/balance  — баланс
```

**Что проверить:**
- Транзакции логируются при каждой операции
- balance_after корректный
- Фильтры по типу/дате работают

---

### Фича 9: Инвойсы (4 теста)
**API endpoints:**
```
GET  /api/invoices           — список
GET  /api/invoices/{id}      — детали
POST /api/invoices           — создать
POST /api/invoices/{id}/pay  — оплатить
```

**Что проверить:**
- CRUD работает
- Повторная оплата → ошибка
- Overdue статус для просроченных

---

### Фича 10: Документы (3 теста)
**API endpoints:**
```
GET /api/documents             — список
GET /api/documents/{id}        — детали
GET /api/documents/{id}/download — URL для скачивания
```

---

### Безопасность (3 теста)
**Что проверить:**
- Все записи имеют company_id
- Админ компании A не видит данные компании B
- Soft delete фильтруется везде

---

## 🏃 КАК РАБОТАТЬ

### Шаг 1: Изучи проект
- Прочитай `bpv/FEATURES_DOCUMENTATION.md`
- Посмотри структуру БД через `mcp_supabase_list_tables`
- Посмотри данные через `mcp_supabase_execute_sql`

### Шаг 2: Тестируй API
Можешь использовать любой способ:

**Через curl в терминале:**
```bash
# Получить токен
curl -X POST https://business-crm-iu04.onrender.com/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"phone": "+992876086666", "password": "azamat6666"}'

# Использовать токен
curl https://business-crm-iu04.onrender.com/api/users \
  -H "Authorization: Bearer <token>"
```

**Через тестовый скрипт (создай если нужно):**
```typescript
const API = 'https://business-crm-iu04.onrender.com/api';

async function login(phone: string, password: string) {
  const res = await fetch(`${API}/auth/login`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ phone, password })
  });
  return res.json();
}

async function testEndpoint(method: string, url: string, token: string, body?: any) {
  const res = await fetch(`${API}${url}`, {
    method,
    headers: {
      'Content-Type': 'application/json',
      'Authorization': `Bearer ${token}`
    },
    body: body ? JSON.stringify(body) : undefined
  });
  return { status: res.status, data: await res.json() };
}
```

### Шаг 3: Проверяй данные в БД
```sql
-- Примеры запросов через mcp_supabase_execute_sql

-- Проверить пользователей
SELECT id, phone, role, status FROM admin_users LIMIT 10;

-- Проверить заказы
SELECT status, COUNT(*) FROM orders GROUP BY status;

-- Проверить заморозки (лимит 2/неделю)
SELECT employee_id, COUNT(*) as freezes_this_week
FROM employee_freeze_history
WHERE freeze_date >= date_trunc('week', CURRENT_DATE)
GROUP BY employee_id;

-- Проверить изоляцию
SELECT company_id, COUNT(*) FROM employees GROUP BY company_id;
```

### Шаг 4: Исправляй баги
Если нашёл баг:
1. Опиши проблему
2. Найди файл с багом
3. Исправь код
4. Проверь что исправление работает

### Шаг 5: Пиши отчёт
После каждой фичи пиши в чат что проверил и какие результаты.

---

## 📊 ФОРМАТ ОТЧЁТА

После каждой фичи:
```
## Фича X: [Название] — ЗАВЕРШЕНО

### API тесты:
| Endpoint | Метод | Тест | Результат |
|----------|-------|------|-----------|
| /auth/login | POST | valid credentials | ✅ 200 |
| /auth/login | POST | invalid password | ✅ 401 |

### БД проверки:
- Уникальность phone: ✅ нет дубликатов
- Soft delete: ✅ работает

### Баги:
- ❌ БАГ: [описание] → 🔧 ИСПРАВЛЕНО: [что сделал]

### Итог: X/X тестов пройдено
```

---

## 🏁 ФИНАЛЬНЫЙ ОТЧЁТ

Когда всё протестируешь, напиши финальный отчёт:

```
# 📊 ФИНАЛЬНЫЙ ОТЧЁТ ТЕСТИРОВАНИЯ

Дата: [дата]

## Сводка:
| Фича | Тестов | ✅ Pass | ❌ Fail | 🔧 Fixed |
|------|--------|---------|---------|----------|
| Auth | 12 | | | |
| Users | 11 | | | |
| Employees | 16 | | | |
| Projects | 8 | | | |
| Dashboard | 13 | | | |
| Lunch | 15 | | | |
| Guest | 6 | | | |
| Transactions | 4 | | | |
| Invoices | 4 | | | |
| Documents | 3 | | | |
| Security | 3 | | | |
| **ИТОГО** | **98** | | | |

## Исправленные баги:
1. [описание] — [файл] — [что исправил]

## Проблемы требующие внимания:
1. [если есть]

## ВЕРДИКТ: ✅ READY FOR PRODUCTION / ❌ NOT READY
```

---

## ⚡ НАЧИНАЙ!

1. Прочитай `bpv/FEATURES_DOCUMENTATION.md`
2. Посмотри данные в БД
3. Начни тестировать с Аутентификации
4. Пиши отчёт после каждой фичи
5. Исправляй баги по ходу
6. Не останавливайся пока не пройдёшь все 98 тестов

**Действуй!**
