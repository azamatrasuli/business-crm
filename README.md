# Yalla Business Admin

> 🍽️ B2B платформа для управления корпоративным питанием сотрудников

## О проекте

**Yalla Business Admin** — система для компаний, которые хотят организовать питание для своих сотрудников. Платформа поддерживает два режима:

- **Комплексные обеды (Lunch)** — подписки на ежедневные обеды с доставкой
- **Компенсации (Compensation)** — возмещение расходов на питание в ресторанах-партнёрах *(в разработке)*

## Архитектура

```
yalla-business-admin/
├── frontend/          # Next.js 15 + React 19 + TypeScript
├── backend/           # ASP.NET Core 8 Web API  
├── docs/              # Документация (API, деплой)
└── assets/            # Бизнес-документация
```

## Возможности MVP

| Модуль | Описание |
|--------|----------|
| **Авторизация** | JWT + Refresh tokens + Импершонация для SUPER_ADMIN |
| **Проекты** | Мультипроектность — компании с изолированными филиалами |
| **Пользователи** | RBAC с ролями admin/manager и настраиваемыми правами |
| **Сотрудники** | HR модуль — штат, бюджеты, подписки на обеды |
| **Подписки** | Комплексные обеды (Комбо 25 / Комбо 35 сомони) |
| **Dashboard** | Операционная панель с заказами и статистикой |
| **Гостевые заказы** | Разовые заказы без подписки |

## Быстрый старт

### Frontend

```bash
cd frontend
npm install
npm run dev
# → http://localhost:3000
```

### Backend

```bash
cd backend
dotnet restore
dotnet run --project src/YallaBusinessAdmin.Api
# → http://localhost:5000/api
# → http://localhost:5000/swagger
```

### Переменные окружения

**Frontend** (`.env.local`):
```env
NEXT_PUBLIC_API_BASE_URL=http://localhost:5000/api
NEXT_PUBLIC_APP_ENV=development
```

**Backend** (`appsettings.json`):
```json
{
  "ConnectionStrings": {
    "DefaultConnection": "Host=localhost;Database=yalla;Username=postgres;Password=..."
  },
  "Jwt": {
    "Secret": "минимум-32-символа-секретный-ключ"
  }
}
```

## Структура проекта

### Frontend
```
frontend/
├── app/                    # Next.js App Router
│   ├── (auth)/            # Страницы авторизации
│   └── (dashboard)/       # Защищённые страницы
├── components/
│   ├── ui/                # UI компоненты (shadcn/ui)
│   ├── layout/            # Сайдбар, хедер
│   └── features/          # Бизнес-компоненты
├── lib/
│   ├── api/               # API клиенты (axios)
│   └── features.config.ts # Feature flags
└── stores/                # Zustand stores
```

### Backend
```
backend/src/
├── YallaBusinessAdmin.Api/           # Controllers, Program.cs
├── YallaBusinessAdmin.Application/   # DTOs, интерфейсы
├── YallaBusinessAdmin.Domain/        # Entities, Enums
└── YallaBusinessAdmin.Infrastructure/# EF Core, сервисы
```

## Окружения

| Среда | Frontend | Backend | БД |
|-------|----------|---------|-----|
| Development | localhost:3000 | localhost:5000 | Supabase Dev |
| Staging | Vercel (develop) | Render Staging | Supabase Staging |
| Production | Vercel (main) | Render Prod | Supabase Prod |

## Документация

- [API.md](docs/API.md) — Документация REST API
- [DEPLOYMENT.md](docs/DEPLOYMENT.md) — Руководство по деплою
- [RELEASE_PROGRESS.md](RELEASE_PROGRESS.md) — Статус релиза

## Тестовый вход

```
Телефон: +992901234567
Пароль: admin123
Роль: SUPER_ADMIN
```

## Лицензия

© 2024-2025 Yalla. Все права защищены.
